services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: englishforu-frontend
    ports:
      - "4200:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - englishforu-network

networks:
  englishforu-network:
    driver: bridge
